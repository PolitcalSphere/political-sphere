Shared library

This placeholder library is tagged with `shared` and intended to contain common
code used across apps (utilities, types, small helpers). It exists so import
boundary rules have a target to validate.
