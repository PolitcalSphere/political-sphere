import{_ as a,c as t,o,ae as s}from"./chunks/framework.CBTkueSR.js";const m=JSON.parse('{"title":"Political Sphere Platform Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md"}'),n={name:"architecture.md"};function i(r,e,l,d,c,p){return o(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="political-sphere-platform-architecture" tabindex="-1">Political Sphere Platform Architecture <a class="header-anchor" href="#political-sphere-platform-architecture" aria-label="Permalink to &quot;Political Sphere Platform Architecture&quot;">​</a></h1><p>Political Sphere runs as an Nx monorepo with three delivery verticals:</p><ul><li><strong>Infrastructure</strong> – Terraform modules that provision the AWS foundation (VPC, EKS, RDS, Redis, Route53, ACM, KMS, IAM, ECR, S3).</li><li><strong>Platform</strong> – Kubernetes Helm charts and Argo CD GitOps definitions for core cluster services and workloads.</li><li><strong>CI</strong> – Reusable GitHub Actions workflows and composite actions that implement build, scan, IaC validation, and GitOps deployment automation.</li><li><strong>Dev Tooling</strong> – Docker Compose / Tilt-based developer stack for local parity.</li></ul><h2 id="high-level-diagram" tabindex="-1">High-Level Diagram <a class="header-anchor" href="#high-level-diagram" aria-label="Permalink to &quot;High-Level Diagram&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>+--------------------+       +-------------------------+</span></span>
<span class="line"><span>| GitHub Actions     |       | AWS Account             |</span></span>
<span class="line"><span>| - build-and-test   |       | - VPC / Subnets         |</span></span>
<span class="line"><span>| - iac-plan         |  OIDC | - EKS Cluster           |</span></span>
<span class="line"><span>| - deploy-argocd    +------&gt;+ - RDS / Redis / S3      |</span></span>
<span class="line"><span>+--------------------+       | - Route53 / ACM / KMS   |</span></span>
<span class="line"><span>                              | - ECR Repositories      |</span></span>
<span class="line"><span>                              +-------------------------+</span></span>
<span class="line"><span>                                         |</span></span>
<span class="line"><span>                                         | IRSA / ArgoCD Sync</span></span>
<span class="line"><span>                                         v</span></span>
<span class="line"><span>                                +-------------------------+</span></span>
<span class="line"><span>                                | Kubernetes (EKS)        |</span></span>
<span class="line"><span>                                | - platform-core chart   |</span></span>
<span class="line"><span>                                |   * ingress-nginx       |</span></span>
<span class="line"><span>                                |   * cert-manager        |</span></span>
<span class="line"><span>                                |   * external-dns        |</span></span>
<span class="line"><span>                                |   * Argo CD             |</span></span>
<span class="line"><span>                                |   * Monitoring Stack    |</span></span>
<span class="line"><span>                                |   * Loki / Tempo / FB   |</span></span>
<span class="line"><span>                                | - App charts (api, ... )|</span></span>
<span class="line"><span>                                | - Namespaces (dev/prod) |</span></span>
<span class="line"><span>                                +-------------------------+</span></span></code></pre></div><h2 id="terraform-module-composition" tabindex="-1">Terraform Module Composition <a class="header-anchor" href="#terraform-module-composition" aria-label="Permalink to &quot;Terraform Module Composition&quot;">​</a></h2><p><code>infrastructure/main.tf</code> wires together reusable modules:</p><table tabindex="0"><thead><tr><th>Module</th><th>Responsibility</th><th>Notes</th></tr></thead><tbody><tr><td><code>modules/vpc</code></td><td>VPC, subnets, NAT, route tables</td><td>Supports NAT toggle per env</td></tr><tr><td><code>modules/eks</code></td><td>Control plane, node groups, IRSA OIDC provider, SGs</td><td>Accepts node group map</td></tr><tr><td><code>modules/rds</code></td><td>PostgreSQL instance, subnet group, parameter group</td><td>Generates final snapshot suffix</td></tr><tr><td><code>modules/redis</code></td><td>ElastiCache replication group</td><td>Optional per env</td></tr><tr><td><code>modules/ecr</code></td><td>Repositories and optional replication</td><td>Enforces scanning</td></tr><tr><td><code>modules/route53</code></td><td>Hosted zone + records</td><td>Works for private/public</td></tr><tr><td><code>modules/acm</code></td><td>ACM certificate with optional DNS validation</td><td>Emits validation records</td></tr><tr><td><code>modules/kms</code></td><td>KMS key with admin/user principals</td><td>Used for RDS/S3 encryption</td></tr><tr><td><code>modules/s3</code></td><td>Secure S3 buckets with lifecycle, logging, replication</td><td>Supports SSE/KMS</td></tr><tr><td><code>modules/iam</code></td><td>GitHub OIDC provider and per-repo roles</td><td>Allows custom inline policy</td></tr></tbody></table><p>Environment overlays (<code>envs/dev</code>, <code>envs/staging</code>, <code>envs/prod</code>) provide opinionated configurations: VPC CIDRs, nodegroup sizing, storage tiers, ACM SANs, bucket naming, and GitHub role policies. Each overlay uses the root module and stores state in S3 with DynamoDB locking (placeholder bucket/table names).</p><h2 id="kubernetes-platform" tabindex="-1">Kubernetes Platform <a class="header-anchor" href="#kubernetes-platform" aria-label="Permalink to &quot;Kubernetes Platform&quot;">​</a></h2><p>The <code>platform/charts/platform-core</code> umbrella chart bundles cluster add-ons:</p><ul><li><strong>Ingress / Networking</strong>: AWS NLB-backed ingress-nginx, cert-manager ClusterIssuers, external-dns with IRSA.</li><li><strong>GitOps</strong>: Argo CD installed and exposed via ingress with Slack notifications enabled.</li><li><strong>Observability</strong>: kube-prometheus-stack (Prometheus, Alertmanager, Grafana w/ Loki + Tempo data sources), Loki distributed logging, Tempo for traces, Fluent Bit DaemonSet shipping logs.</li><li><strong>Policy / Namespaces</strong>: Namespace manifests with resource quotas, LimitRanges, default NetworkPolicies for <code>dev</code>, <code>staging</code>, <code>prod</code>, and <code>ci</code>.</li></ul><p>Application charts (<code>api</code>, <code>frontend</code>, <code>worker</code>, <code>auth</code>, <code>db-proxy</code>) all follow the same pattern: Helm-managed Deployment + Service + HPA + optional ConfigMap/Ingress. Values files per environment set image tags and IRSA annotations. Argo CD Applications reference the charts with <code>values-[env].yaml</code> overlays.</p><h2 id="ci-cd-flow" tabindex="-1">CI/CD Flow <a class="header-anchor" href="#ci-cd-flow" aria-label="Permalink to &quot;CI/CD Flow&quot;">​</a></h2><ol><li><strong>Build/Test</strong> (<code>ci/workflows/build-and-test.yaml</code>) <ul><li>Run Nx lint/test/build targets.</li><li>Build a container image, scan with Trivy, push to ECR (if AWS secrets provided).</li><li>Publish built image URI as an artifact.</li></ul></li><li><strong>Release Orchestration</strong> (<code>ci/workflows/application-release.yaml</code>) <ul><li>Calls the build workflow.</li><li>Reads the pushed image tag and invokes <code>deploy-argocd</code> to sync the target application with the new tag.</li></ul></li><li><strong>IaC Validation</strong> (<code>ci/workflows/iac-plan.yaml</code>) <ul><li>Uses composite action <code>setup-terraform</code> for consistent versions.</li><li>Runs <code>terraform fmt</code>, <code>tflint</code>, <code>tfsec</code>, <code>checkov</code>, and optionally <code>terraform plan</code> under an OIDC-assumed role.</li></ul></li><li><strong>Argo CD Sync</strong> (<code>ci/workflows/deploy-argocd.yaml</code>) <ul><li>Installs argocd-cli, logs in using API token, optionally applies parameter overrides, syncs &amp; waits for health.</li></ul></li></ol><h2 id="local-development" tabindex="-1">Local Development <a class="header-anchor" href="#local-development" aria-label="Permalink to &quot;Local Development&quot;">​</a></h2><ul><li><code>dev/docker/docker-compose.dev.yaml</code> mirrors production services (API, frontend, worker, Keycloak-based auth, PostgreSQL, Redis, LocalStack, MailHog, pgAdmin).</li><li><code>dev/scripts/dev-up.sh</code> / <code>dev-down.sh</code> manage the stack. <code>seed-data.sh</code> runs migrations/seeding using Prisma or fallback scripts.</li><li><code>dev/templates/.env.example</code> and <code>.env.local.example</code> provide environment bootstrap.</li><li>Optional <code>dev/Tiltfile</code> runs compose via Tilt for better visibility.</li></ul><h2 id="secrets-identity" tabindex="-1">Secrets &amp; Identity <a class="header-anchor" href="#secrets-identity" aria-label="Permalink to &quot;Secrets &amp; Identity&quot;">​</a></h2><ul><li>GitHub Actions assume AWS roles via OIDC (<code>modules/iam</code>).</li><li>Kubernetes workloads rely on IRSA (roles annotated in Helm values) and Vault / Secrets Manager integration (to be populated later).</li><li>S3 buckets default to private ACLs with SSE and optional KMS keys.</li></ul><h2 id="disaster-recovery-backups" tabindex="-1">Disaster Recovery &amp; Backups <a class="header-anchor" href="#disaster-recovery-backups" aria-label="Permalink to &quot;Disaster Recovery &amp; Backups&quot;">​</a></h2><ul><li>RDS module enforces snapshots (<code>final_snapshot_identifier</code>) and supports Multi-AZ for upper environments.</li><li>S3 module allows lifecycle rules to tier/expire objects.</li><li>Compose stack includes <code>localstack</code> to validate infrastructure automation offline.</li><li>Future documentation (see <code>runbooks/incident.md</code>) details backup/restoration runbooks.</li></ul><h2 id="future-enhancements" tabindex="-1">Future Enhancements <a class="header-anchor" href="#future-enhancements" aria-label="Permalink to &quot;Future Enhancements&quot;">​</a></h2><ul><li>Add GitOps policy enforcement (Kyverno/Gatekeeper) once admission policies are defined.</li><li>Extend Argo CD Applications to helmfile or app-of-apps pattern if repo splitting occurs.</li><li>Integrate Vault ExternalSecrets when secrets backend is selected.</li></ul>`,23)])])}const h=a(n,[["render",i]]);export{m as __pageData,h as default};
