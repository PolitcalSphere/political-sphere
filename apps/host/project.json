{
  "name": "host",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/host",
  "projectType": "application",
  "tags": ["app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Build host app (copy Module Federation config into your builder)'"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Serve host app (configure webpack-dev-server with MF)'"
      }
    }
  }
}
