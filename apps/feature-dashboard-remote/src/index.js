// STATUS: PENDING_IMPLEMENTATION
// Entry point for dashboard remote

export function mount(root) {
	const el = document.createElement("div");
	el.textContent = "Dashboard Remote Placeholder";
	root.appendChild(el);
}
